    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

          <% if user_signed_in? %>
            <a class="navbar-brand" href="/"><%= current_user.name %>の俳句</a>
          <% else %>
            <a class="navbar-brand" href="/">俳句投稿サイト</a>
          <% end %>
          
        </div>
        <div class="collapse navbar-collapse" id="navigation">
          <% if user_signed_in? %>
            <p class="navbar-text navbar-right"><%= link_to("俳句を投稿する", new_haiku_path) %></p>
            <p class="navbar-text navbar-right"><%= link_to("退出", destroy_user_session_path, method: :delete) %></p>
            &nbsp;
          <% else %>
            <p class="navbar-text navbar-right"><%= link_to("俳人の登録", new_user_registration_path) %></p>
            &nbsp;
            <p class="navbar-text navbar-right"><%= link_to("入室", new_user_session_path) %></p>
          <% end %>
        </div>
      </div>
    </nav>